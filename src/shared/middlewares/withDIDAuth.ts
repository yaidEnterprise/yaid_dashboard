import { NextRequest, NextResponse } from "next/server";

// Stub — implementação completa na Story 5.1 (Epic 5)
export function withDIDAuth(_request: NextRequest): NextResponse {
  return NextResponse.next();
}
